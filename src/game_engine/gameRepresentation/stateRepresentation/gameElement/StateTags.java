package game_engine.gameRepresentation.stateRepresentation.gameElement;

/**
 * A temporary file to allow for the important tags of states to be moved outside of individual
 * classes in the state hierarchy. Should eventually be moved to a config file or interface of
 * sorts.
 * 
 * @author Zach
 *
 */
// TODO move this into a config file or equivalent
public class StateTags {
    public static final String NAME_ATTRIBUTE_STRING = "Name";
    public static final String TYPE_ATTRIBUTE_STRING = "Type";

}
